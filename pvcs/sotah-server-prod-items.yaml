apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sotah-server-prod-items-pvc
spec:
  storageClassName: faster
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
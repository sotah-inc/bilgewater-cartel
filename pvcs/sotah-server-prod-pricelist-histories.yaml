apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sotah-server-prod-pricelist-histories-pvc
spec:
  storageClassName: faster
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi